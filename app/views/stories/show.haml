.alert.alert-info.fade.in.hide
  %button.close{"data-dismiss" => "alert"} ×
  %strong Yay!
  Your invitations have been sent. Lets hope they are as skilled as you...

%h2 A story started by #{@story.user.first_name}

.story
  - @story.lines.each do |line|
    = render :partial => "line", :locals => {:line => line}
  = render :partial => "add_line"

